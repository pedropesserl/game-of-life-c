#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int ROWS;
int COLS;

void imprime_matriz(int matriz[ROWS][COLS]) {
    for (int i = 0; i < ROWS; i++) {
        for (int j = 0; j < COLS; j++)
    }
}

int main() {
    int grid[ROWS][COLS]

    return 0;
}
